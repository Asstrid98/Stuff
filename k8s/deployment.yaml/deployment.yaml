apiVersion: apps/v1
kind: Deployment
metadata:
  name: taylinn
  labels:
    app: taylinn
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taylinn
  template:
    metadata:
      labels:
        app: taylinn
    spec:
      containers:
      - name: python-app
        # Esta imagen la creará GitHub automáticamente
        image: ghcr.io/saronos/taylinn:latest 
        ports:
        - containerPort: 5000
        imagePullPolicy: Always
        env:
        - name: MODO_OPERATIVO
          value: "NORMAL"
        livenessProbe: # El sistema vigilará si la app responde
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
